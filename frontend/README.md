# JKmart 99

Neighborhood electronics store web app powered by React, Vite, Tailwind CSS, and Supabase.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set environment variables in [.env](.env)
3. Run the development server:
   `npm run dev`
